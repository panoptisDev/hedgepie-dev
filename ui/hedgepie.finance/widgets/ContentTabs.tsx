import React from 'react'

interface ContentTabsProps {
  tabs: [{ head?: React.ReactNode; content?: React.ReactNode }]
}

function ContentTabs(props: ContentTabsProps) {
  return <div>ContentTabs</div>
}

export default ContentTabs
